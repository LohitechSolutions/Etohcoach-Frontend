EXPO_PUBLIC_API_BASE_URL=https://etohcoachfinal-159129-ruby.b159129.dev.eastus.az.svc.builder.cafe

# Set to "true" to call the real login API. Omit or "false" = tap Sign In to enter the app without auth (testing only).
EXPO_PUBLIC_USE_REAL_AUTH=false

# If you see "Network request failed" with real auth: ensure this URL is reachable from the device (physical phones cannot use http://localhost — use your machine's LAN IP).

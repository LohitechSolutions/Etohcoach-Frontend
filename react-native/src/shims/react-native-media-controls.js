import React from "react";

export const PLAYER_STATES = {
  PAUSED: 0,
  PLAYING: 1,
  ENDED: 2,
};

/** Stub overlay controls — install the real package for playback UI. */
export default function MediaControls() {
  return null;
}
